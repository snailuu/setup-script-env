# 更新日志

## [v1.2.1] - 2025-12-17

### 🐛 Bug 修复

#### Homebrew 5.0.x Go 链接问题
- **问题**：Homebrew 5.0.6 存在 Go formula bug
  - 错误：`Error: missing keywords: :date, :because`
  - 导致：`brew link go` 失败
- **修复**：
  - 智能检测 Homebrew bug
  - 提供交互式解决方案
  - 选项 1：自动卸载并重新安装 Go
  - 选项 2：跳过，稍后手动修复
- **影响**：用户能绕过 Homebrew bug 完成安装

#### oh-my-zsh 兼容性问题
- **问题**：bash 脚本中 `source ~/.zshrc` 加载 oh-my-zsh
  - 错误：`ZSH_VERSION: unbound variable`
  - 原因：oh-my-zsh 需要 zsh 环境
- **修复**：
  - 不在脚本中 source 配置文件
  - 只 export 当前 shell 需要的变量
  - 配置写入文件，重启终端后生效
- **影响**：使用 oh-my-zsh 的用户不再遇到错误

### 🔧 改进

#### 智能 Homebrew Bug 绕过
- 检测到 Homebrew bug 时，提供三层兜底方案：
  1. 尝试重新安装 Go
  2. 重装失败 → 使用 gvm binary 模式
  3. gvm 失败 → 提示手动安装
- 完全避免因 Homebrew bug 导致安装失败

#### 错误信息显示
- 捕获并显示所有命令的 stderr
- 显示退出码
- 针对已知问题提供解决方案
- 实时显示安装输出

## [v1.2.0] - 2025-12-17

### 🐛 重大 Bug 修复

#### Go 脚本错误显示问题
- **问题**：安装失败时不显示具体错误信息
  - 原因：`2>/dev/null` 隐藏了所有错误输出
  - 原因：`set -u` 导致 gvm 脚本加载失败
- **修复**：
  - 移除 `set -u`，避免 gvm 兼容性问题
  - 改 `2>/dev/null` 为 `2>&1` 捕获错误
  - 显示详细的错误信息（前 10 行）
  - 提供具体的解决建议
- **影响**：用户现在能看到失败原因和解决方案

#### Shell 检测问题
- **问题**：检测脚本运行环境而非用户环境
  - 用户使用 zsh，但配置写入 `.bash_profile`
  - 配置不生效，安装失败
- **修复**：
  - 使用 `$SHELL` 环境变量检测用户 shell
  - 三层兜底机制（$SHELL → 文件检测 → .profile）
  - 正确写入配置文件
- **影响**：所有用户的配置都能正确生效

#### 转义字符显示问题
- **问题**：输出中显示 `\033[0;36m` 而非彩色文本
- **修复**：为所有颜色输出的 `echo` 添加 `-e` 参数
- **影响**：界面显示正常

### ✨ 新增功能

#### Go 版本管理交互
- 检测到已安装 Homebrew Go 时，提供交互式选择
  - 选项 1：保留现有 Go（作为 bootstrap）
  - 选项 2：卸载现有 Go（完全用 gvm 管理）
- 二次确认防止误操作
- 清晰的选项说明和建议

### 🗑️ 项目精简

#### 删除的文件（9 个）
- 测试脚本：`demo.sh`、`diagnose-go.sh`、`verify-go.sh`
- 冗余文档：`QUICKSTART.md`、`GO_GUIDE.md`、`TROUBLESHOOTING.md`
- 技术文档：`QUICK_FIX.md`、`SHELL_DETECTION.md`、`GO_VERSION_MANAGEMENT.md`、`FINAL_SUMMARY.md`

#### 文档整合
- 将 9 个文档的核心内容整合到 `README.md`
- 保持简洁但完整
- 包含：快速开始、使用指南、FAQ、故障排查

### 🔧 改进

#### 错误处理
- 所有脚本移除 `set -e` 和 `set -u`
- 使用手动错误检查，提供更好的错误信息
- 临时禁用 `set -u` 避免 gvm 兼容性问题

#### 用户体验
- 更详细的错误信息
- 更友好的解决建议
- 实时显示安装输出
- 显示退出码帮助调试

## [v1.1.0] - 2025-12-17

### 🐛 Bug 修复

#### Go 脚本 (setup-go.sh)
- **修复**：错误使用 `gvm listall` 检查已安装版本
- **修复**：添加安装后验证

#### Python 脚本 (setup-python.sh)
- **新增**：安装后立即验证
- **新增**：环境变量立即生效

#### Rust 脚本 (setup-rust.sh)
- **新增**：安装后立即验证
- **新增**：环境文件存在性检查

#### 主安装脚本 (install.sh)
- **修复**：兼容 Bash 3.2（macOS 默认版本）

### ✨ 新增功能

- 完整的错误检查和处理
- 安装后立即验证
- 友好的错误提示

## [v1.0.0] - 2025-12-16

### ✨ 初始版本

- 交互式安装界面
- 支持 Go (gvm)、Python (uv)、Rust (rustup)
- 可视化 CLI 界面
- 国内镜像加速
- 完整的文档

---

## 版本说明

- **v1.2.0**: 错误显示修复 + 项目精简
- **v1.1.0**: Bug 修复和兼容性改进
- **v1.0.0**: 初始发布

## 贡献者

- @snailuu - 所有版本开发和维护
